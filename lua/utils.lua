local M = {
  fs = require("utils.fs"),
  array = require("utils.array"),
  cmd = require("utils.cmd"),
  packs = require("utils.packs"),
  package_manager = require("utils.package_manager"),
}


return M
